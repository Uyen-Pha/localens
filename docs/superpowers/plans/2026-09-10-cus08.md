# UC-CUS08 implementation
1. Add owner-scoped read/sync RPC, expiry job, cancel eligibility and one-review-per-completed-booking storage. Preserve original payment history.
2. Add /booking-details/?booking=... with authenticated lookup, error/retry, conditional payment/cancel/review forms. Replace inline list detail with navigation.
3. Immediate local search/filter/sort; poll owner rows every 15s and focus, explicit empty/error states, refresh on clear.
4. Verify DB ownership, expiry, 48-hour boundary, duplicate review; component tests and browser; build then apply migration and promote Vercel. Do not publish unrelated edits.
